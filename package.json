{
  "name": "warp-ticker",
  "version": "0.0.3",
  "description": "Configurable timer component for 'warp'",
  "main": "./dist/index.js",
  "scripts": {
    "build": "rm -rf ./dist && babel --source-maps=true --extensions='.es6' --copy-files -d ./dist ./",
    "example": "npm run build && cd ./dist && node ./example/index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/potrata/warp-ticker.git"
  },
  "dependencies": {
    "node-warp": "^1.0.5"
  },
  "devDependencies": {
    "babel": "^5.x.x",
    "babel-runtime": "^5.x.x"
  },
  "keywords": [
    "warp",
    "component",
    "ticker"
  ],
  "author": "Nikolay Chursin <nick.chursin@gmail.com>",
  "license": "ISC"
}
